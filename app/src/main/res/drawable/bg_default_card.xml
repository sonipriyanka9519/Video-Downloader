<?xml version="1.0" encoding="utf-8"?>
<!-- The panel the three benefits sit in. White against the sheet's warm fade, so the list reads
     as one block lifted off the colour behind it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <solid android:color="@color/default_card" />
    <corners android:radius="@dimen/_16sdp" />
</shape>
