<?xml version="1.0" encoding="utf-8"?>
<!-- The queue sheet's slab. Dark whatever theme the app is in — it is raised over video. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ds_player_sheet" />
    <corners
        android:topLeftRadius="@dimen/ds_radius_sheet"
        android:topRightRadius="@dimen/ds_radius_sheet" />
</shape>
