<?xml version="1.0" encoding="utf-8"?>
<!-- The disc behind the fingerprint. Tappable, so it needs a ground to be seen as a control. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="oval">
    <solid android:color="@color/ds_lock_sensor" />
</shape>
