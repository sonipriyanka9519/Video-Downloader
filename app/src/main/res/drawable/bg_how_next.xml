<?xml version="1.0" encoding="utf-8"?>
<!-- The only thing to do on the walkthrough, so the only thing filled in. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <gradient
        android:angle="0"
        android:endColor="@color/how_next_end"
        android:startColor="@color/how_next_start"
        android:type="linear" />

    <corners android:radius="@dimen/_26sdp" />
</shape>
