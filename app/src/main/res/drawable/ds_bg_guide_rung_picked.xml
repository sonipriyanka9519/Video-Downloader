<?xml version="1.0" encoding="utf-8"?>
<!--
  The quality rung being chosen, in the last illustration: accent-soft with an accent border, which
  is exactly how a chosen rung reads on the real sheet.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ds_accent_soft" />
    <stroke
        android:width="@dimen/ds_stroke_selected"
        android:color="@color/ds_accent" />
    <corners android:radius="@dimen/ds_radius_thumb" />
</shape>
